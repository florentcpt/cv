version: 2

jobs:
  build:
    docker:
      # Primary container
      - image: mirisbowring/texlive_ctan_full:2019
    steps:
      - checkout
      - run: git config --type bool --add http.sslverify 'false for github.com'  
      - run: git submodule sync
      - run: git submodule update --init
      - run:
          name: Compile
          command: make
      - store_artifacts:
          path: ~/dist
